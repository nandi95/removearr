services:
  removearr:
    image: nandi95/removearr:main
    container_name: removearr
    env_file: .env
    ports:
      - "8484:8484"
    volumes:
      - ./data:/app/.data
    restart: unless-stopped
